<h2><%= t(:new_product) %></h2>

<%= labelled_form_for @product, url: project_products_path do |f| %>
  <fieldset class="box tabular">
    <legend><%=t(:product_info)%></legend>
      <%= render 'form', f: f %>
      <p><%= submit_tag :new, value: t(:new_product) %></p>
  </fieldset>
<% end %>